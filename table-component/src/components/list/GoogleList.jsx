import React, { Fragment } from "react";
import BackButton from "../button/BackButton";

const GoogleList = () => {
  return (
    <Fragment>
      <BackButton />
    </Fragment>
  );
};

export default GoogleList;
